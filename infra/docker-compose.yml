services:
  db:
    image: postgres:16
    container_name: gentlix-postgres
    environment:
      POSTGRES_USER: mihai
      POSTGRES_PASSWORD: admingentlix01
      POSTGRES_DB: gentlix_bank
    ports:
      - "5432:5432"
    volumes:
      - gentlix_pgdata:/var/lib/postgresql/data

volumes:
  gentlix_pgdata:
